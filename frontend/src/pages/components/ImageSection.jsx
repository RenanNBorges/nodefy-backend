import React from 'react';
import styled from 'styled-components';

const ImageSection = () => {
    return (
        <ImageContainer>
            {/* Adicione o componente de upload de imagem aqui */}
        </ImageContainer>
    );
};

const ImageContainer = styled.div`
  flex: 1;
  padding: 20px;
`;

export default ImageSection;
